# Extra HTTP servers go here (listen on the WireGuard address only).
# Rebuild the gateway image after edits. Example:
#
# server {
#     listen 10.13.13.1:8443;
#     server_name _;
#     location / {
#         proxy_pass http://127.0.0.1:3000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     }
# }
