# Example HTTP reverse-proxy for web apps on this host.
# Copy to a name ending in .conf and rebuild the gateway image.
# Listen on the tunnel IP only. Never 0.0.0.0 (that would be public).
#
# Safari/Chrome on iPad/Android: http://10.13.13.1/  (IPv4, after WireGuard is up)
#
# server {
#     listen 10.13.13.1:80;
#     server_name _;
#     server_tokens off;
#     location /mfa/auth { internal; proxy_pass http://127.0.0.1:9090/auth; proxy_pass_request_body off; proxy_set_header Content-Length ""; proxy_set_header X-Forwarded-For $remote_addr; }
#     location @mfa_login { return 302 /mfa/; }
#     location / {
#         include /etc/nginx/mfa-protect.inc;
#         proxy_pass http://127.0.0.1:8080;
#         proxy_set_header Host $host;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
